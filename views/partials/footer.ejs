<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mock Quiz Hub Footer</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.2/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #1a1f2e;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
        }

        .content {
            flex: 1;
            padding: 50px 0;
        }

        .footer {
            background-color: #1a202c;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            color: #94a3b8;
            padding: 2rem 0;
            margin-top: auto;
        }

        .footer-content {
            text-align: center;
        }

        .footer-text {
            font-size: 0.95rem;
            margin: 0;
            letter-spacing: 0.5px;
        }

        .heart {
            color: #e74c3c;
            animation: heartbeat 1.5s ease-in-out infinite;
            margin: 0 8px;
            font-size: 1.1rem;
        }

        .creator-name {
            color: #60a5fa;
            font-weight: 600;
            text-decoration: none;
            transition: all 0.3s ease;
            position: relative;
        }

        .creator-name:hover {
            color: #93c5fd;
            text-shadow: 0 0 10px rgba(96, 165, 250, 0.3);
        }

        .creator-name::after {
            content: '';
            position: absolute;
            bottom: -2px;
            left: 0;
            width: 0;
            height: 2px;
            background: linear-gradient(90deg, #60a5fa, #93c5fd);
            transition: width 0.3s ease;
        }

        .creator-name:hover::after {
            width: 100%;
        }

        @keyframes heartbeat {
            0%, 50%, 100% {
                transform: scale(1);
            }
            25%, 75% {
                transform: scale(1.1);
            }
        }

        /* Demo content styling */
        .demo-content {
            color: #e2e8f0;
            text-align: center;
        }

        .demo-title {
            color: #f1f5f9;
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 1rem;
        }

        .demo-subtitle {
            color: #94a3b8;
            font-size: 1.1rem;
            margin-bottom: 3rem;
        }
    </style>
</head>
<body>
   
    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <div class="footer-content">
                <p class="footer-text">
                    Made with <i class="fas fa-heart heart"></i> by 
                    <a href="https://www.linkedin.com/in/rashmika-nawanjana-88b6b2300/" target="_blank" rel="noopener noreferrer" class="creator-name">Rashmika Nawanjana</a>
                </p>
                <p class="footer-text">collaborators: Kalana Lakshan </p>
            </div>
        </div>
    </footer>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.2/js/bootstrap.bundle.min.js"></script>
</body>
</html>