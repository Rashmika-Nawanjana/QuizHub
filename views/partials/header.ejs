<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mock Quiz Hub</title>
    
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/header.css">
    
    <style>
        /* CSS Variables for Light/Dark Mode */
        :root {
            --icon-bg: rgba(255, 255, 255, 0.9);
            --icon-border: rgba(255, 255, 255, 0.95);
            --icon-shadow: rgba(0, 0, 0, 0.08);
            --icon-shadow-hover: rgba(86, 36, 208, 0.25);
            --icon-shimmer: rgba(255, 255, 255, 0.6);
            --notification-color: #5624d0;
            --leaderboard-gradient: linear-gradient(135deg, #ff6b35 0%, #f7931e 100%);
            --profile-gradient: linear-gradient(135deg, #5624d0 0%, #7e3ff2 100%);
            --status-online: #10b981;
            --notification-badge: #ef4444;
        }

        [data-theme="dark"] {
            --icon-bg: rgba(30, 30, 50, 0.9);
            --icon-border: rgba(60, 60, 80, 0.7);
            --icon-shadow: rgba(0, 0, 0, 0.4);
            --icon-shadow-hover: rgba(124, 58, 237, 0.4);
            --icon-shimmer: rgba(255, 255, 255, 0.3);
            --notification-color: #a78bfa;
            --leaderboard-gradient: linear-gradient(135deg, #fbbf24 0%, #f59e0b 100%);
            --profile-gradient: linear-gradient(135deg, #8b5cf6 0%, #a855f7 100%);
            --status-online: #34d399;
            --notification-badge: #f87171;
        }

        /* Premium Icon Button Base */
        .premium-icon-btn {
            background: var(--icon-bg);
            border: 1px solid var(--icon-border);
            border-radius: 16px;
            padding: 0.8rem;
            backdrop-filter: blur(20px);
            box-shadow: 
                0 8px 32px var(--icon-shadow),
                inset 0 1px 0 rgba(255, 255, 255, 0.5),
                0 1px 0 rgba(255, 255, 255, 0.6);
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            color: var(--text-primary, #1c1d1f);
            text-decoration: none;
            position: relative;
            overflow: hidden;
            display: flex;
            align-items: center;
            justify-content: center;
            width: 52px;
            height: 52px;
            border: 2px solid transparent;
        }

        /* Advanced shimmer effect */
        .premium-icon-btn::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: conic-gradient(from 0deg, transparent 0%, var(--icon-shimmer) 50%, transparent 100%);
            opacity: 0;
            transition: all 0.6s ease;
            animation: rotate 3s linear infinite;
        }

        @keyframes rotate {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        /* Hover state with multiple effects */
        .premium-icon-btn:hover {
            transform: translateY(-4px) scale(1.05);
            box-shadow: 
                0 16px 48px var(--icon-shadow-hover),
                inset 0 1px 0 rgba(255, 255, 255, 0.8),
                0 0 0 2px rgba(86, 36, 208, 0.3);
            text-decoration: none;
            border-color: rgba(86, 36, 208, 0.4);
        }

        .premium-icon-btn:hover::before {
            opacity: 1;
        }

        /* Individual icon styling */
        .notification-icon {
            color: var(--notification-color);
            font-size: 1.3rem;
            filter: drop-shadow(0 2px 4px rgba(86, 36, 208, 0.3));
            transition: all 0.3s ease;
        }

        .premium-icon-btn:hover .notification-icon {
            color: #7c3aed;
            filter: drop-shadow(0 4px 8px rgba(124, 58, 237, 0.4));
            transform: scale(1.1);
        }

        .leaderboard-icon {
            background: var(--leaderboard-gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            font-size: 1.3rem;
            filter: drop-shadow(0 2px 4px rgba(255, 107, 53, 0.3));
            transition: all 0.3s ease;
        }

        .premium-icon-btn:hover .leaderboard-icon {
            background: linear-gradient(135deg, #fbbf24 0%, #f59e0b 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            filter: drop-shadow(0 4px 8px rgba(251, 191, 36, 0.4));
            transform: scale(1.1) rotate(5deg);
        }

        .profile-icon {
            background: var(--profile-gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            font-size: 1.3rem;
            filter: drop-shadow(0 2px 4px rgba(86, 36, 208, 0.3));
            transition: all 0.3s ease;
        }

        .premium-icon-btn:hover .profile-icon {
            background: linear-gradient(135deg, #8b5cf6 0%, #a855f7 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            filter: drop-shadow(0 4px 8px rgba(139, 92, 246, 0.4));
            transform: scale(1.1);
        }

        /* Enhanced status indicator */
        .profile-status {
            position: absolute;
            top: 4px;
            right: 4px;
            width: 14px;
            height: 14px;
            background: var(--status-online);
            border: 2px solid white;
            border-radius: 50%;
            box-shadow: 
                0 2px 8px rgba(16, 185, 129, 0.4),
                inset 0 1px 2px rgba(255, 255, 255, 0.3);
            animation: statusPulse 3s infinite;
        }

        @keyframes statusPulse {
            0%, 100% { 
                opacity: 1;
                transform: scale(1);
            }
            50% { 
                opacity: 0.7;
                transform: scale(1.1);
            }
        }

        /* Premium notification badge */
        .notification-badge {
            position: absolute;
            top: -4px;
            right: -4px;
            background: linear-gradient(135deg, var(--notification-badge) 0%, #dc2626 100%);
            color: white;
            font-size: 0.7rem;
            font-weight: 700;
            border-radius: 50%;
            width: 22px;
            height: 22px;
            display: flex;
            align-items: center;
            justify-content: center;
            border: 3px solid white;
            box-shadow: 
                0 4px 12px rgba(239, 68, 68, 0.4),
                inset 0 1px 2px rgba(255, 255, 255, 0.3);
            animation: badgePulse 2s infinite;
        }

        @keyframes badgePulse {
            0% {
                transform: scale(1);
                box-shadow: 0 4px 12px rgba(239, 68, 68, 0.4), 0 0 0 0 rgba(239, 68, 68, 0.4);
            }
            70% {
                transform: scale(1.05);
                box-shadow: 0 4px 12px rgba(239, 68, 68, 0.6), 0 0 0 8px rgba(239, 68, 68, 0);
            }
            100% {
                transform: scale(1);
                box-shadow: 0 4px 12px rgba(239, 68, 68, 0.4), 0 0 0 0 rgba(239, 68, 68, 0);
            }
        }

        /* Active/Focus states */
        .premium-icon-btn:active {
            transform: translateY(-2px) scale(1.02);
            transition: all 0.1s ease;
        }

        .premium-icon-btn:focus {
            outline: none;
            box-shadow: 
                0 8px 32px var(--icon-shadow),
                0 0 0 3px rgba(86, 36, 208, 0.3),
                inset 0 1px 0 rgba(255, 255, 255, 0.5);
        }

        /* Dark mode enhancements */
        [data-theme="dark"] .premium-icon-btn {
            box-shadow: 
                0 8px 32px var(--icon-shadow),
                inset 0 1px 0 rgba(255, 255, 255, 0.1),
                0 1px 0 rgba(255, 255, 255, 0.05);
        }

        [data-theme="dark"] .premium-icon-btn:hover {
            box-shadow: 
                0 16px 48px var(--icon-shadow-hover),
                inset 0 1px 0 rgba(255, 255, 255, 0.2),
                0 0 0 2px rgba(124, 58, 237, 0.4);
        }

        [data-theme="dark"] .notification-badge {
            border-color: #1a1a2e;
        }

        [data-theme="dark"] .profile-status {
            border-color: #1a1a2e;
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            .premium-icon-btn {
                width: 48px;
                height: 48px;
                padding: 0.7rem;
            }
            
            .notification-icon,
            .leaderboard-icon,
            .profile-icon {
                font-size: 1.2rem;
            }
            
            .notification-badge {
                width: 20px;
                height: 20px;
                font-size: 0.65rem;
            }
            
            .profile-status {
                width: 12px;
                height: 12px;
            }
        }

        /* Loading animation for icons */
        .premium-icon-btn.loading {
            pointer-events: none;
        }

        .premium-icon-btn.loading i {
            animation: iconSpin 1s linear infinite;
        }

        @keyframes iconSpin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
    </style>
</head>
<body>
    
    <header class="py-4" style="background-color: var(--github-secondary-bg); border-bottom: 1px solid var(--github-border); transition: background-color 0.3s ease, border-color 0.3s ease;">
        <div class="container">
            <div class="d-flex align-items-center justify-content-between flex-wrap flex-md-nowrap" style="gap: 1rem; min-height: 56px;">
                <!-- Left: Theme Toggle -->
                <div class="theme-toggle-container order-1 order-md-1">
                    <button class="theme-toggle" id="themeToggle" aria-label="Toggle theme">
                        <i class="theme-icon fas fa-moon" id="themeIcon"></i>
                        <span class="theme-text" id="themeText">Dark</span>
                    </button>
                </div>
                
                <!-- Center: Title -->
                <h1 class="navbar-brand mb-0 mx-auto order-2 order-md-2 text-center" style="font-size: 2rem; white-space: nowrap; flex: 1 1 0;">
                    <a href="/" class="no-style">
                        <i class="fas fa-graduation-cap me-2"></i>Mock Quiz Hub
                    </a>
                </h1>
                
                <!-- Right: Icons -->
                <div class="d-flex align-items-center order-3 order-md-3 ms-auto position-relative" style="gap: 1rem;">
                    
                    <!-- Notification Icon -->
                    <div class="notification-container position-relative">
                        <button class="premium-icon-btn" id="notificationDropdown" aria-expanded="false">
                            <i class="fas fa-bell notification-icon"></i>
                            <span class="notification-badge">1</span>
                        </button>
                        <div class="notification-tooltip">
                            <div class="fw-bold mb-2">Recent Updates</div>
                            <div class="small mb-1">Added: OS Mid 1 Quiz</div>
                            <div class="small mb-1">Added: OS Mid 2 Quiz</div>
                            <div class="small mb-1">Added: OS Lab 1 Quiz</div>
                            <div class="dropdown-divider my-1"></div>
                            <div class="dropdown-item-text text-muted small">Check back for more updates!</div>
                        </div>
                    </div>
                    
                    <!-- Leaderboard Icon -->
                    <a href="/leaderboard" class="premium-icon-btn" title="Leaderboard">
                        <i class="fas fa-trophy leaderboard-icon"></i>
                    </a>
                    
                    <!-- Profile Icon -->
                    <% if (typeof user !== 'undefined' && user) { %>
                        <a href="/dashboard" class="premium-icon-btn">
                            <i class="fas fa-user profile-icon"></i>
                            <div class="profile-status"></div>
                        </a>
                    <% } else { %>
                        <a href="/auth/login" class="premium-icon-btn">
                            <i class="fas fa-user profile-icon"></i>
                        </a>
                    <% } %>
                    
                </div>
            </div>
        </div>
    </header>

    <script>
        const themeToggle = document.getElementById('themeToggle');
        const themeIcon = document.getElementById('themeIcon');
        const themeText = document.getElementById('themeText');
        const body = document.body;

        // Check for saved theme preference or default to light
        const currentTheme = localStorage.getItem('theme') || 'light';
        
        // Apply saved theme on load
        if (currentTheme === 'dark') {
            body.setAttribute('data-theme', 'dark');
            themeIcon.className = 'theme-icon fas fa-sun';
            themeText.textContent = 'Light';
        }

        themeToggle.addEventListener('click', () => {
            const isDark = body.getAttribute('data-theme') === 'dark';
            
            // Add switching animation
            themeIcon.classList.add('switching');
            
            // Toggle theme
            if (isDark) {
                body.removeAttribute('data-theme');
                themeIcon.className = 'theme-icon fas fa-moon';
                themeText.textContent = 'Dark';
                localStorage.setItem('theme', 'light');
            } else {
                body.setAttribute('data-theme', 'dark');
                themeIcon.className = 'theme-icon fas fa-sun';
                themeText.textContent = 'Light';
                localStorage.setItem('theme', 'dark');
            }

            // Remove animation class after animation completes
            setTimeout(() => {
                themeIcon.classList.remove('switching');
            }, 600);
        });
    </script>
</body>
</html>